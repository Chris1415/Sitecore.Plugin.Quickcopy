# Ship Report — FRD-001 (20260620T122930Z)

**Work item type:** frd
**Product:** quickcopy (`products/marketplace/quickcopy`)
**Branch:** `frd-001` → PR to `main`
**Prepared by:** Product Manager (02), constructive mode
**Ship status:** `shipped_with_caveats`

## One-line

A single ~200 ms trailing-edge debounce on the panel's URL-prefetch trigger so
rapid page-to-page navigation collapses to one settled prefetch trio — all five
FRD acceptance criteria delivered and fully test-covered; the only caveat is the
real-tenant host-frame smoke, which is deferred (no live tenant in the autonomous
run), correctly forcing `shipped_with_caveats`.

## What was specified vs delivered (FRD acceptance criteria)

| Acceptance criterion | Classification | Evidence |
|----------------------|----------------|----------|
| AC1 — Rapid switching across N pages issues ≪ 3×N calls; only the settled trio fires | delivered | Test "collapses rapid distinct id changes to ONE settled prefetch (last id wins)" — PASS (test report § Requirement Coverage) |
| AC2 — Settled page resolves all three slots (copy/live/share) exactly as before | delivered | Test "still fires exactly once for a settled single page"; `prefetch.ts` + cards unchanged; full suite green |
| AC3 — Back/forward to a previously-fetched id resolves from cache, zero new calls | delivered | Existing cache-hit regression test still PASS post-change |
| AC4 — Debounce interval is a single named constant (default ~200 ms) | delivered | `PREFETCH_DEBOUNCE_MS = 200` in `marketplace.tsx` |
| AC5 — Existing suite green + new test asserts rapid-id collapse | delivered | 24 files / 179 tests pass (176 baseline + 3 new) |
| Real-tenant host-frame behavior (far fewer calls, settled page resolves, ~200 ms feels OK) | deferred | `smoke_outcomes.host_frame_smoke: deferred` — no live tenant in autonomous run |

All five testable ACs are **delivered and covered**. No AC is partial or cut.

## What shipped (code)

- `site/next-app/components/providers/marketplace.tsx` — added `PREFETCH_DEBOUNCE_MS = 200`
  and wrapped the existing `prefetchPageUrls(...)` dispatch (inside the cache-key
  effect) in a trailing-edge `setTimeout`, with `clearTimeout` in the effect
  cleanup. Cache-hit short-circuit, missing-input bails, and `requireContextId`
  try/catch all preserved in place.
- `site/next-app/components/providers/marketplace.prefetch.test.tsx` — 3 new tests
  (fake-timer `describe`): collapse-to-one (last id wins), settled-single-page
  still fires once, unmount-mid-window cancels the pending fetch.

No new dependency; no abstraction added (lean `architecture_budget` honored). No
change to cache keying, cache invalidation, slot composition, `prefetchPageUrls`,
or share-link formats.

## What was deferred

- **Real-tenant host-frame / live-walkthrough smoke** — requires a live tenant +
  Cloud Portal host URL not available in the autonomous run. Recorded as
  `smoke_outcomes.host_frame_smoke: deferred`. **Open follow-up:** operator runs
  the panel in Cloud Portal, confirms rapid page-switching issues far fewer SDK
  calls, a settled page still resolves copy/live/share, and the ~200 ms delay
  feels acceptable; then flips the smoke outcome to `passed` (or files a tune if
  laggy). Until then this run stays `shipped_with_caveats`.

## Test coverage state

- Lint: PASS. Build (`next build` 16.1.7): PASS. Typecheck: PASS.
- Vitest: **24 files / 179 tests pass** (0 fail, 0 skip). 3 new debounce tests.
- No E2E runner in the repo (N/A).

## Known limitations

- Debounce *feel* (200 ms) is unverified against a human on a real tenant — tunable
  via the single `PREFETCH_DEBOUNCE_MS` constant if smoke finds it laggy (FRD § 7).

## Technical debt introduced

- None identified. The change is a localized, well-commented refinement consistent
  with ADR-0006 / ADR-0007.

## ADR compliance

| ADR | Decision | Honored? |
|-----|----------|----------|
| ADR-0001 | ADRs as architecture backbone | Yes — no new ADR needed (FRD § 6, implementation refinement) |
| ADR-0002 | Extension point: Pages context panel | Yes — unchanged |
| ADR-0003 | Blok as UI framework | Yes — no UI change |
| ADR-0004 | Clipboard local copy, no shared package | Yes — unchanged |
| ADR-0005 | Marketplace scaffold (client-side, flattened) | Yes — unchanged |
| ADR-0006 | Live URL composition — parallel prefetch on `pageInfo.id` change | Yes — this FRD is its named "low-risk follow-up" debounce; prefetch strategy unchanged, only debounced |
| ADR-0007 | Cache invalidation — version-keyed, no TTL | Yes — cache keying + invalidation untouched |
| ADR-0008 | Keyboard shortcut scheme | Yes — unchanged |
| ADR-0009 | Error-state policy — no retry | Yes — unchanged |
| ADR-0010 | Share-link split-button composition | Yes — unchanged |

## Open follow-ups

1. **[caveat]** Run real-tenant host-frame smoke for FRD-001; flip
   `smoke_outcomes.host_frame_smoke` to `passed` once verified (see "What was
   deferred").
2. **[backlog]** TASK-002 / FRD-002 — `/live`-slot 30 s TTL (out of scope here;
   still `todo` in `tasks.md`).

## What hurt this run

No friction log was configured for this run (`friction_log.path: null`). No
friction events captured. One minor environmental note (not blocking): the
`security_reminder_hook` string-matches React HTML-injection keywords even inside
documentation prose — the code-review report had to be reworded to avoid the
literal keyword. (Already a known item in operator memory.)

## Recommendation

Merge the PR. FRD-001's acceptance criteria are fully delivered and test-covered;
the single caveat is the deferred real-tenant smoke. After merge, run the
host-frame smoke on a live tenant to clear the caveat and (optionally) close
TASK-001 as fully verified.
